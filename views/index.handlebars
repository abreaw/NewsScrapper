
<p class="subtitle">Get a list of jobs.  Input the data below and click "Get Jobs".</p>

<form action="/scrape" id="scrape-data">

    <div class="field">
        <div class="field">
            <label class="label">Keywords</label>
            <div class="control">
                <input type="text" name="keyword" placeholder="junior developer" autofocus>
            </div>
            <p class="help">Enter keywords for search.</p>
        </div>
        
        <div class="field">
            <label class="label">Location</label>

            <div class="control">
                <input type="text" name="location" placeholder="wake forest, nc">
            </div>
        </div>
        
        <div class="field">
            <div class="control">
                <input class="button is-info" type="submit" value="Get Jobs">
            </div>
        </div>
    </div>

</form>


